fun int double(a int) {
    return a*2;
}

fun int plustwo(a int) => a + 2
->
let plustwo = (a int): int => a + 2

let x = double(2) + 1 + ((a int) => a + 4)(3)

let x = map((a int) => a*2, range(5));

let x = map((a int) => { let b = 5 + 1; return a * b; }, range(5));
